%h1 Listing users

%table.table
  %thead
    %tr
      %th メールアドレス
      %th ニックネーム
      %th
      %th
      %th

  %tbody
    - @users.each do |admin_user|
      %tr
        %td= user.email
        %td= user.nickname
        %td= link_to 'Show', admin_user_path
        %td= link_to 'Edit', edit_admin_user_path(user)
        %td= link_to 'Destroy', admin_user_path, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
